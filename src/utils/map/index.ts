import mapkey from './mapkey';
import Map from './map';

// map数据更新
// import mapTransform from './mapTransform'
// console.log(JSON.stringify(mapTransform.mapArr), 'u')

export {
  Map,
  mapkey,
};
